# Changelog

## [Unreleased]

### Added

- Added the initial v1 cross-language contracts and deterministic JSON Schema generator.
- Added the Docker Compose bootstrap doctor, strict Controller health boundary, machine-readable evidence report,
  and fail-closed CLI.
- Added strict v1 run-artifact contracts, hash-linked events, versioned experiment capacity plans, and fail-closed
  `run --dry-run --config` matrix accounting.
- Added the M1 container lifecycle, independent evaluator, CNY cost accounting, immutable run evidence, and
  self-contained static HTML report for the frozen Axios task.
